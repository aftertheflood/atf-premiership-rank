{% extends "page.html.nj" %}

{% block content %}
<div class="content-block intro">{{ 'intro' | md | safe }}</div>
<div class="content-block">
  {{ data.results | table({
    columns: ['club','points','position','wagebill','_rank','_rankDifference'],
    headings: ['','Points','Position','Wages (£ millions)','ATF Rank',''],
    columnClasses: ['','number','number','number millions','number','number color'],
    sort: 'points',
    rowClass: 'code'
  }) | safe }}
</div>
<div class="content-block indepth">{{ 'how' | md | safe }}</div>
<object type="image/svg+xml" data="/scatter-plot.svg" class="logo">scatter</object>
<div class="content-block badges">{% include "badges.html.nj" %}</div>
<div class="content-block intro">{{ 'footer' | md | safe }}</div>
{% endblock %}
